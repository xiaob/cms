<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shishuo.jiawacms.dao.ConfigDao">
	
	<select id="allConfig" 
		resultType="com.shishuo.jiawacms.entity.Config">
		select * from cms.config limit #{offset},#{rows}
	</select>
	
	<select id="getAllConfig" 
		resultType="com.shishuo.jiawacms.entity.Config">
		select * from cms.config
	</select>

	
	<insert id="addConfig" parameterType="com.shishuo.jiawacms.entity.Config">
		insert into
		cms.config(`key`,`value`,createTime)
		value(#{key},#{value},#{createTime})
	</insert>
	
	<delete id="deleteConfig" parameterType="com.shishuo.jiawacms.entity.Config">
		delete from cms.config where `key`=#{key}
	</delete>
	
	<update id="updateConfig" parameterType="com.shishuo.jiawacms.entity.Config">
		update cms.config set value=#{value}
		where `key`=#{key}
	</update>
	
	<select id="getConfigByKey"
		resultType="com.shishuo.jiawacms.entity.Config">
		select * from cms.config where `key`=#{key}
	</select>
</mapper>  
